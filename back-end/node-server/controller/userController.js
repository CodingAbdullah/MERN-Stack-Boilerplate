const express = require("express");
const User = require("../model/User"); // Make use of the User model in the user controller

// Create API end points using the controller
exports.loginFormValidation = (req, res) => {
    // Authenticate with mongoose models 
};

exports.signUpFormValidation = (req, res) => {

};